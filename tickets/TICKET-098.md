# TICKET-098: Verify docs/CLI.md and README.md flag tables remain accurate

**Cycle:** 25
**Files:** `docs/CLI.md`, `README.md`

No new flag is introduced this cycle (the flags already exist in `helpText()`
and `parseArgv`). Verify the existing flag tables in `docs/CLI.md` and
`README.md` remain accurate and do not add duplicate rows.
